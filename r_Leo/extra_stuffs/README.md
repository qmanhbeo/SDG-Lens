# Extra Materials

This folder contains supporting materials kept for reference:

- `1_author_original_refactored.py`
- `1_author_original_refactored_notebook.ipynb`
- `paper.pdf`

These files are archival and are not the primary marking entry points for the submission.

For the actual submission workflow, use the repository root:

1. `README.md` for setup and run instructions
2. `main.py` for the minimal replication
3. `main_full_paper.py` for the full replication
4. `replication_results/` for the generated outputs

This separation is intentional so the archive material does not override the current submission instructions.

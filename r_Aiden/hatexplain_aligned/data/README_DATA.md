This package fetches the public HateXplain files automatically from the official repository.

Files downloaded at runtime:
- `dataset.json`
- `post_id_divisions.json`

The package records SHA-256 checksums in `outputs/results.json` so the exact data used in the run are auditable.
